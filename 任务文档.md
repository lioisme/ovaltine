# 任务文档：在一加 Ace Pro（ovaltine / PGP110）上编出 LineageOS ROM

仓库：`lioisme/ovaltine` · 机型代号：`ovaltine`（国行 PGP110 = 海外 OnePlus 10T）· 移植基线：`lineage-ovaltine-dev`
本文档是唯一执行清单：每项带验收门（Gate）与执行记录；未打勾即未完成。

## 0. 最终验收门

| 门 | 判据 | 状态 |
|---|---|---|
| G-A 资料齐备 | 底包/设备树/内核/blob/工具全部到手且经校验（哈希或 diff） | ✅ 2026-09-25 |
| G-B 编译可复现 | 一条 `workflow_dispatch` 从空 runner 到产物，无人工干预 | ⏳ 进行中（run 36157950244，跨 job 续建第 1 轮） |
| G-C 产物合法 | 生成可刷 `lineage*.zip`（或 `*_ota.zip`），带 SHA256，≥1GB，且 Actions 归档 | ⬜ |
| G-D 国行身份 | 产物内 `PRODUCT_MODEL=PGP110`、指纹条目用国行文件、固件混刷脚本可用 | ⚠️ 补丁已就绪，待 G-C 后启用 |
| G-E 实机可用 | 刷入 Ace Pro 后：开机、蜂窝、相机、指纹、高刷、快充、NFC、GPS 全活（回归表） | ⬜ 需实机 |

> G-B 的判据已按实测修订：单个 hosted job 硬上限 6 小时、盘 ~108G，装不下一整轮
> LineageOS 23.2 构建（同步就要数小时），所以「一条 dispatch」允许被拆成若干轮接力，
> 人工干预仍为零 —— 由 `chain.yml` 自动排下一轮，直到出包或触到轮数上限。

## 1. 阶段任务

### P1 资料与判定（已完成）
| ID | 任务 | 验收 | 状态 | 执行记录 |
|---|---|---|---|---|
| 1.1 | 确定机型身份 | 代号/板级/SoC 有字节级证据 | ✅ | 底包 metadata `PGP110→OP5551L1`、`CPH2415→OP5552L1`；dtb `qcom,waipio-*`；`board-info.txt` 含两者 |
| 1.2 | 拉取全部移植仓库 | 本地可查、分支可枚举 | ✅ | 40 仓库 / 20.4GB → `sources/`；清单见《资料清单.md》 |
| 1.3 | 判定官方支持状态 | wiki data + 仓库内容 | ✅ | 无 `ovaltine.yml`；`LineageOS/android_device_oneplus_ovaltine` 为空仓（API 409） |
| 1.4 | 国行 vs 国际差异 | 逐文件哈希/diff | ✅ | dts 侧 A15 全树 diff：仅 `oplus_chg` 4 文件、ovaltine 差 1 行；blob 侧 2599 需求 = 2316 同 / 266 异 / 8 仅社区 / 9 仅国行 |
| 1.5 | 底包可解性 | 分区表 + 固件枚举 | ✅ | 46 分区；`proprietary-firmware.txt` 要的 24 个固件 100% 存在；分区是 EROFS |
| 1.6 | 固件版本风险 | 提交史 + 哈希比对 | ✅ | 国行 `abl/xbl/xbl_config` 哈希 ≠ 设备树 pin（OOS13）；pin 原因=新 xbl 打死 EDL |

### P2 CI 化编译（进行中）
| ID | 任务 | 验收 | 状态 | 执行记录 |
|---|---|---|---|---|
| 2.1 | 建 local_manifest | 各仓库目标分支真实存在 | ✅ | 逐个 API 校验；`vendor/extra` 用 `lineage-21.0` 平台名，社区树用 `lineage-21`；GitLab 无 `lineage-22.0` |
| 2.2 | 写工作流 | `actionlint` 0 错 | ✅ | `.github/workflows/build.yml`；`build.sh` 走 `m dist` 且兼容 A15/A16 的 lunch 三段式 |
| 2.3 | 依赖与 repo 工具 | 步骤 success | ✅ | 逐包容错安装（`libesd0-dev`/`python3-pyyaml` 在 24.04 不存在） |
| 2.4 | 源码同步成功 | sync 步骤 success 且关键仓库存在校验通过 | ⏳ | 失败根因实测两条：① `No space left on device`（108G 峰值不够）② run 12 的 200 分钟同步时限被吃满（分批 + `-j8` 串行太慢）。现策略：单发 `-j32`，超时靠 `repo sync` 可续性接着拉，磁盘紧张才分批回收对象库（run 36157950244 验证中） |
| 2.5 | 编译成功 | `out` 出 zip | ⬜ | 待 2.4 |
| 2.6 | 产物归档 | artifact 可下载 + SHA256 | ⬜ | 只收 zip，不收 `super.img` 等裸镜像 |
| 2.7 | 算力兜底 | 找到能跑完的 x86_64 Linux | ✅ 判定完成 | hosted 唯一可行：手机/NAS 是 arm64（见 2.9），Win 本机无 WSL/Hyper-V 且禁在 win 编；larger-runner 无额度（实测 queued 不启动） |
| 2.8 | 跨 job 续建 | 单轮干不完能把进度接力，无人工干预 | ⏳ 管道已验，第 15 轮在跑 | 本地干跑通过：save-tree→save-out→probe→get→五处文件 md5 全 SAME（tree/out 两条腿都验）。踩到的实测坑：本仓 step 环境**没有**自动注入的 `GITHUB_TOKEN`，`gh` 直接报「set the GH_TOKEN」→ Release 建不出来、存档/进度全静默失败；已在 job 级 `env.GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` 修掉（自检 workflow run 36162144860 的日志坐实） |
| 2.9 | 编译主机架构判定 | 有字节级依据，不靠猜 | ✅ | AOSP 只有 `prebuilts/clang/host/linux-x86` 与 `prebuilts/go/linux-x86`（lineage-23.2 manifest 里 `linux-arm64` 出现 0 次）；`prebuilts/build-tools` 根目录只有 `linux-x86 / darwin-x86 / linux_musl-x86 / linux_musl-arm64`，没有 glibc 的 `linux-arm64` → arm64 主机（手机 proot Ubuntu、NAS）编不动 A14+ 的 LineageOS |
| 2.10 | 同步源可达性 | 两端各测一次 | ✅ | `android.googlesource.com` 从 Windows 与本机构内手机均 TCP 超时（lineage manifest 的 `remote="aosp"` 直指它）→ 国内节点无法整机同步；Actions runner 可达，故源码侧只能走 Actions |
| 2.11 | 容器 SSH 9002 | 用户能直接 ssh 进 Ubuntu | ✅ | 容器内 sshd 在 proot 下起不来（privsep 要 setuid）→ 改为 Termux 再起一个原生 sshd 实例监听 9002，`ForceCommand proot-distro login ubuntu -- bash -l`；从 Windows 探 9002 OPEN，登录后落在 Ubuntu；已写 `~/.termux/boot/02-sshd-9002.sh` 开机自启。8022 不受影响（只按 pidfile 管自己那个实例） |

| 2.12 | 同步速率与磁盘上限 | 有实测曲线，能算出裁剪量 | ✅ | run 36169431905（裁剪 66 项前 42 项）：**同步 62 分钟完成 1131 个项目**，工作树 74G、`.repo` 靠每批回收稳在 0.6-4G，收尾 avail=30G。对照未裁剪的 run 36167028022：12 分钟就掉到 avail=4G。tree 存档 36 片 ×1.5G ≈ 54G（zstd-3）。**结论：out/ 只有 ~30G 空间 → 必须再裁**（module_sdk 24 项 + 非 pin 的 clang 版本已在 slim.sh/cull.xml 里，第 2 轮起生效） |

### 2.1 续建链的实测节奏（2026-09-25 夜，run 19 起）

| 环节 | 实测 | 备注 |
|---|---|---|
| repo init + 分批同步 | ~62 min | 1131 项目 / `-j16` / 12 批 / 每批回收 `.repo/project-objects` |
| prune + slim | ~3 min | 删 git 元数据；只留被引用的 clang 版本 |
| tree 存档上传 | ~35 min | 36 × 1.5G 分片，边压边传边删 |
| 编译（第 1 轮剩余额度） | ~2.5 h | 到点 `timeout -s INT` 优雅中断，进度进 out 存档 |
| out 存档 | 待测 | 第 1 轮末尾产出；第 2 轮 `get` 取回后续跑 |

**运维纪律（本轮踩到的）**

1. `gh run cancel` 之后 runner 上的进程可能不立刻死 —— 实测被取消的 run 36167028022 在
   取消后 70-90 分钟仍在往 Release 传 `tree.tar.zst.partNN` 并改 Release 正文。
   所以：**能不取消就不取消**；要改用等下一轮自动生效（chain 每轮都重新 checkout main 的脚本）。
2. 分片族名必须带 run id（`tree-<run>.partNNN`）：共用族名时，僵尸轮的「上传完清理旧序号」
   会把别人的尾巴删掉、或用自己不同的字节覆盖同名片 → 下一轮拼出坏档。已改；
   `get` 侧另加护栏：实到片数 ≠ state 记的片数就**拒绝解包**（坏档不能进编译）。
3. `gh release view --json id` 给的是 GraphQL node id（`RE_xxx`），拼
   `/releases/{id}/assets` 会 404；数字 id 要从 `gh api repos/{o}/{r}/releases/tags/{tag}` 取。
4. `GET /repos/{o}/{r}/releases/{id}` 的 `assets` 数组**只给前 30 条**（API 截断），
   一族 36+ 片时必须走 `/releases/{id}/assets?per_page=100` 分页端点，否则列举漏尾 →
   自家分片被当成「过期」删掉。
5. 分片数以「本轮 put-part 本地记账」为准，不以远端列举为准。
6. 跑中的 job 取不到部分日志（logs 端点 BlobNotFound）→ 一切可观测量都得自己旁路透出
   （Release 正文 + 每步 df）。


### P3 国行化（依赖 G-C）

| ID | 任务 | 验收 | 状态 | 执行记录 |
|---|---|---|---|---|
| 3.1 | 应用 PGP110 补丁 | `patch -p1 --fuzz=0 --dry-run` 通过 | ✅ | 已在本地 23.2 设备树实测：`lineage_ovaltine.mk`/`vendor.prop`/`proprietary-files.txt` 三个文件全部干净套用；CI 用 `use_cn_patches` 开关（同步/取回之后、prune 之前） |
| 3.2 | 自提国行 blob | `extract-files.py` 产物指纹哈希 = `1ea1131b…` | ⬜ 需在真实 ext4 的 Linux 上做（proot 下 `shutil.move` 触 `Read-only file system`，已实测） |
| 3.3 | 定位栈决策 | GPS 实测通过 | ⬜ 默认沿用社区混源 V5 栈；异常再回退国行 V3 栈 |
| 3.4 | 固件混刷 | 脚本内 3 个 pin 哈希校验通过 | ✅ `flash-firmware.sh`（刷入需实机） |

### P4 实机与回归（需设备）
| ID | 任务 | 验收 | 状态 |
|---|---|---|---|
| 4.1 | BL 解锁 + 备份 `abl/xbl/xbl_config/oplusstanvbk` | `dd` 副本留存 | ⬜ |
| 4.2 | 刷 G-C 产物 | 首次开机成功 | ⬜ |
| 4.3 | 回归表（蜂窝/相机/指纹/高刷/快充/NFC/GPS/蓝牙/USB/音频/传感器/省电） | 逐项记录，缺陷回 P3 | ⬜ |
| 4.4 | 上游反馈 | 向 `lineage-ovaltine-dev` 提 PGP110 差异（指纹 manifest、21841 充电属性） | ⬜ |

## 2. 已定的关键技术决策（含依据）

1. **基线 = 社区 `lineage-ovaltine-dev`**，不从零参考：其 23.2 分支源码活跃（2025-12/2026-01 有提交），且 dts 实测证明 Ace Pro 与 10T 硬件描述几乎完全一致。
2. **第一轮不启用国行补丁**：社区 blob 树里没有 `manifest_oplus_fingerprint_aidl.xml`，先出包再谈国行化。
3. **固件混刷**：21 个用国行 15.0，`abl/xbl/xbl_config` 用社区 `radio/` 的 OOS13 pin 版（哈希已核）。整包刷 15.0 会换掉新 xbl → 失去 EDL。
4. **版本路线**：先 `lineage-21.0`（唯一有社区成品包佐证的分支）或 `lineage-23.2`（维护中分支）。当前工作流默认 23.2。
5. **编译必须在 x86_64 Linux**：AOSP 只有 `prebuilts/clang/host/linux-x86`，arm64 主机（手机、NAS）无 host 预编译，实测标准 hosted runner 磁盘不足。

## 3. 复现入口

```
# CI：Actions → Build ROM → Run workflow（branch / runner / variant / 开关）
# 本地：
repo init -u https://github.com/LineageOS/android -b lineage-23.2 --depth=1 --git-lfs
sed -e 's/@PLATFORM@/lineage-23.2/g' -e 's/@COMMUNITY@/lineage-23.2/g' \
    local_manifests/ovaltine.xml > .repo/local_manifests/ovaltine.xml
repo sync -c --no-tags -j8
bash ci/build.sh userdebug        # 或本地 bash build.sh userdebug
```

自检脚本（本机）：`bash .fetch/verify.sh` → 3 个 bootloader pin、blob 缺口、两份底包在位。
