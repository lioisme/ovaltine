# 一加 Ace Pro（ovaltine / PGP110）LineageOS 适配：资料清单与缺口分析

生成时间 2026-09-25 · 本机落点 `D:/Users/liois/Documents/lioisme/lineageos/acepro/sources/`

## 结论摘要

1. **一加 Ace Pro 与 OnePlus 10T 是同一台机器**：代号 `ovaltine`，SoC SM8475（waipio/Cape），国行型号 PGP110，海外 CPH2413/2415/2417/2419。板级标识实测：**OP5551L1 = PGP110（国行）**、**OP5552L1 = CPH2415（国际）**。主摄同为 IMX766（两侧官方 dts 与底包文件清单实测一致），面板/触控/指纹同一份 DTBO。
2. **LineageOS 官方不支持 ovaltine**，只有社区移植 `lineage-ovaltine-dev`，分支一路做到 **lineage-23.2（Android 16）**，另有 `amitkumar2007` 的 lineage-23.1 分支可交叉验证。
3. 需要的资料 **绝大部分已在 GitHub/GitLab 权威仓库，本次已全部落盘（38 个仓库 / 19GB）**；两份 15.0.0.1901 底包（国行 PGP110 + 国际 CPH2415）也已到位。**唯一还缺的是 PGP110 的 MSM 深度刷机（救砖）包**，以及 LineageOS 上游平台源码（~150-200GB，只能在 Linux 构建机上 repo sync）。
4. blob 完整性实测：设备树要求 1543 个闭源文件，社区 GitLab 23.2 blob 覆盖 1541，只有 2 条差异，且**都能从本次已落盘的其他分支补齐**。
5. 本目录**不能用来构建**：Windows/NTFS 大小写与保留名问题（实测 10 个内核树首次 checkout 全部失败），**本机未装 WSL**，且两份底包落地后 D 盘只剩 48GB——连 AOSP 源码量的 1/3 都不够。本目录的定位是"资料仓 + 底包/dts 比对基准"。

---

## 一、机型判定（先定身份，资料才不会选错）

| 项目 | 结论 | 证据（本地落盘文件） |
|---|---|---|
| 代号 | `ovaltine` | `sources/lineageos/device_oneplus_ovaltine/lineage_ovaltine.mk`；DTBO 节点 `qcom,mdss_dsi_oplus_ovaltine_boe_nt37705_1080_2412_dsc_cmd` |
| 国行型号 | **PGP110**（含原神限定版） | 型号表 `KHwang9883/MobileModels/brands/oneplus.md` |
| 海外型号 | CPH2413(IN)/CPH2415(EU)/CPH2417(NA)/CPH2419(TMO) | 同上；`proprietary-firmware.txt` 头注 `CPH2415_15.0.0.1601(EX01)` |
| SoC | Snapdragon 8+ Gen 1 = **SM8475**，平台 waipio/Cape | PGP110 dtb 内 `qcom,waipio-*` compatible；OnePlusOSS 内核分支命名 `sm8475_*_ace_pro` |
| 板级标识 | `taro` / `OP5551L1` / `OP5552L1` | `device/oneplus/ovaltine/board-info.txt` |
| 主摄 | IMX766（+ ov08d10 超广、ov02b10 微距、s5k3p9 前摄） | Ace Pro dts：`imx766`×71/`ov08d10`/`ov02b10`；10T dts：`imx766`/`ov08d`/`ov02b`/`s5k3p9`；blob：`com.qti.sensormodule.qtech_imx766_main.bin` |
| 显示/触控/指纹 | BOE NT37705 1080×2412 DSC + r66451(144Hz) overlay；Goodix GT9966 触控；屏下 Goodix FP | PGP110 `dtbo.img` strings；`dump_CPH2417/dts/dtbo/*.dts` |
| LineageOS 官方支持 | **无** | `LineageOS/lineage_wiki/docs/_data/devices/` 无 `ovaltine.yml`；`LineageOS/android_device_oneplus_ovaltine` 为空仓库（API 返回 "Git Repository is empty"） |
| 最接近的官方机型 | OnePlus 10 Pro `kebab`（SM8450，官方分支已到 **lineage-24.0**） | `sources/lineageos/device_oneplus_kebab_official`（12 个分支） |
| 现成移植包 | `lineage-21.0-*-UNOFFICIAL-Pig(-Gapps)-ovaltine.zip` 等 | `lineage-ovaltine-dev/release` releases + `CHANGELOG.MD` |

---

## 二、资料分层清单（需要什么 / 从哪拿 / 本地落点 / 状态）

状态图例：✅ 本次已落盘 · ⚠️ 已落盘但需按 PGP110 改 · ❌ 未落盘（附获取方式）

### L0 前置条件
| 资料 | 来源 | 状态 |
|---|---|---|
| BL 解锁资格（国行"深度测试"通道）、当前 ColorOS 版本、电量>70%、USB 3 线 | 一加社区/官方 | ❌ 设备侧动作 |
| fastboot/adb 平台工具 | Google platform-tools | ❌ 需自装（非 Git 资料） |
| 当前固件反回滚索引 `oplusstanvbk` 版本 | 本机 `getprop` | ❌ 刷机前必须记录 |

### L1 硬件描述（DTB / DTBO / dts）
| 资料 | 来源 | 本地落点 | 状态 |
|---|---|---|---|
| **PGP110 原生 kernel/dtb/dtbo**（49MB/956KB/24MB） | `Abandon-Project/twrp_device_oneplus_PGP110` | `sources/community/abandonproject_twrp_PGP110/prebuilt/PGP110/` | ✅ 唯一直接来自国行机的硬件描述 |
| Ace Pro 官方内核 dts/dtso（A13/A14/A15） | `OnePlusOSS/android_kernel_oneplus_sm8475` + `..._modules_and_devicetree_oneplus_sm8475` | `sources/kernel/oneplusoss_sm8475_acepro_A1{3,4,5}`、`oneplusoss_modules_dt_sm8475_acepro` | ✅ |
| 10T 对照内核（A12 原始 + A15） | 同上 `..._10t_5g` / `..._oneplus_10t` | `sources/kernel/oneplusoss_sm8475_10t_A12`、`_10t_A15`、`oneplusoss_modules_dt_sm8475_10t` | ✅ |
| GKI common 内核（5.10，A15 ace_pro） | `OnePlusOSS/android_kernel_common_oneplus_sm8475` | `sources/kernel/oneplusoss_common_sm8475_acepro_A15` | ✅ |
| 10T 全量底包解包参考（含 dtb/dtbo 反编译 dts、各分区 build.prop、audio xml） | `lineage-ovaltine-dev/dump_CPH2417`（CPH2417_13.1.0.584） | `sources/lineageos/dump_CPH2417/` | ✅ |
| 厂商 dtso 分组（camera/display/audio/cvp/mmrm/video） | `lineage-ovaltine-dev/proprietary_qcom_vendor_kernel-*-devicetree` | `sources/lineageos/vendor_kernel_*` | ✅ |

### L2 底包 / 固件（fastboot 分区镜像，非 AOSP 部分）
| 资料 | 来源 | 状态 |
|---|---|---|
| 需要哪些固件镜像（25 个：abl/aop/aop_config/bluetooth/cpucp/devcfg/dsp/engineering_cdt/featenabler/hyp/imagefv/keymaster/modem/oplus_sec/oplusstanvbk/qupfw/shrm/splash/tz/uefi/uefisecapp/xbl/xbl_config/xbl_ramdump） | `device/oneplus/ovaltine/proprietary-firmware.txt`（含 abl/xbl/xbl_config 的 SHA1 锚定） | ✅ 清单已落盘 |
| **镜像本体：社区 `vendor/oneplus/ovaltine` 的 `radio/` 目录，24 个 .img / 360MB** | GitLab 同仓 | ✅ 实测 `radio/abl|xbl|xbl_config` 的 SHA1 与设备树 pin 逐一吻合 |
| **PGP110 官方全量 OTA** `PGP110_15.0.0.1901(CN01)`，Android 15，payload.bin 未加密 | 用户自备 | ✅ 项目根目录（7.22GB） |
| **CPH2415 国际版 OTA** `CPH2415_15.0.0.1901(EX01)` | 用户自备 | ✅ 项目根目录（6.50GB） |
| PGP110 **MSM 深度刷机（救砖）包** | OEM 渠道 / 社区镜像 | ❌ 仍然缺，且是唯一兜底手段（见 G15） |
| payload.bin 解包 | `ssut/payload-dumper-go` | ✅ `sources/tools/payload-dumper-go` |
| 国行 `.ozip` 解密（本次两包都不需要） | `bkerler/oppo_ozip_decrypt` | ✅ `sources/tools/oppo_ozip_decrypt` |
| 10T 底包解包参考 | `lineage-ovaltine-dev/dump_CPH2417`（CPH2417_13.1.0.584） | ✅ `sources/lineageos/dump_CPH2417/`（配置/dts/build.prop，**不含 .img**） |

### L3 闭源 blob（vendor/odm/system 分区文件）
| 资料 | 来源 | 本地落点 | 状态 |
|---|---|---|---|
| ovaltine + sm8450-common blob 全量（23.2 分支 1577 文件） | **GitLab** `lineage-ovaltine-dev/proprietary_vendor_oneplus/{ovaltine,sm8450-common}`（7 分支） | `sources/vendor/gitlab_ovaltine`、`gitlab_sm8450-common` | ✅ |
| 第二套 blob 镜像（20/21/22.x/23.x/23.1） | `amitkumar2007/proprietary_vendor_oneplus_ovaltine` | `sources/community/amitkumar2007_vendor_ovaltine` | ✅ |
| blob 清单与 fixup（namespace/软链/ELF 修补） | `device/oneplus/ovaltine/{proprietary-files.txt,extract-files.py,setup-makefiles.py}` | 同上设备树 | ✅ |
| 提取工具链 | `LineageOS/android_tools_extract-utils` | `sources/lineageos/tools_extract-utils` | ✅ |
| 同 SoC 参考 blob（udon / OnePlus 11R·Ace 2, SM8475） | `Gokulgethu/proprietary_vendor_oneplus_sm8475-common` | `sources/community/gokulgethu_vendor_sm8475-common` | ✅ 仅对照用 |
| 官方渠道 blob | `TheMuppets/proprietary_vendor_oneplus_ovaltine` | — | ❌ **空占位仓库**，官方 LineageOS 侧无 ovaltine blob |

实测差集：设备树需要 1543 条，GitLab 23.2 命中 1541。缺 `odm/lib64/librfapis.so`（23.2 分支已丢，**在已落盘的 22.2 分支与 amitkumar 23.1 树里都有**）与 `vendor/lib64/libRGBC.so`（实际存放于 `odm/lib64/`，属分区路径差异，非缺文件）。

### L4 设备树 / HAL 配置
| 资料 | 来源 | 本地落点 | 状态 |
|---|---|---|---|
| 设备树 ovaltine（分支 20/21/22.1/22.2/23.2 + 实验分支） | `lineage-ovaltine-dev/android_device_oneplus_ovaltine` | `sources/lineageos/device_oneplus_ovaltine` | ⚠️ 身份写死 CPH2415/CPH2417，需改 PGP110 |
| 平台公共树 sm8450-common（15 分支，含 audio/gps/init/location/overlay、VINTF `manifest_taro.xml`/`framework_manifest.xml`、`qcril-database`、`modules.load*`、`{vendor,odm,product,system_ext}.prop`） | `lineage-ovaltine-dev/android_device_oneplus_sm8450-common` | `sources/lineageos/device_oneplus_sm8450-common` | ✅ |
| 一加/OPPO 专用 HAL（fingerprint、Keymaster 包装、触摸、colorbalance…）**含 `hardware/oplus/sepolicy/qti`** | `LineageOS/android_hardware_oplus` @lineage-23.2 | `sources/lineageos/hardware_oplus` | ✅ |
| 早期/备用移植（作分支演化参考） | `tangalbert919/{android_device_oneplus_ovaltine,android_device_oneplus_sm8450-common}`、`amitkumar2007/android_device_oneplus_ovaltine` | `sources/community/*` | ✅ |
| 官方 sibling 模板 kebab（10 Pro，SM8450，官方已到 24.0） | `LineageOS/android_device_oneplus_kebab` | `sources/lineageos/device_oneplus_kebab_official` | ✅ |

### L5 内核
| 资料 | 来源 | 本地落点 | 状态 |
|---|---|---|---|
| 移植实际使用内核（GKI 5.10 + oplus DTB 配置，分支 lineage-20→23.2） | `lineage-ovaltine-dev/android_kernel_oneplus_sm8450{,-devicetrees,-modules}` | `sources/kernel/lod_kernel_*`（22.2 工作区 + 23.2 分支对象） | ✅ |
| 上游官方内核（kebab 用） | `LineageOS/android_kernel_oneplus_sm8450` @lineage-22.2 | `sources/kernel/lineageos_kernel_oneplus_sm8450` | ✅ |
| GKI 模块打包 | `lineage-ovaltine-dev/android_oneplus_kernel_gki-modules` | `sources/lineageos/oneplus_kernel_gki-modules` | ✅ |
| 关键构建参数 | `BoardConfigCommon.mk`：`gki_defconfig vendor/waipio_GKI.config vendor/oplus/waipio_GKI.config vendor/debugfs.config` + `TARGET_KERNEL_CONFIG += vendor/oplus/ovaltine.config`，`TARGET_KERNEL_EXT_MODULE_ROOT=kernel/oneplus/sm8450-modules`，`BOARD_AVB_ENABLE` 用 AOSP testkey | 已落盘 | ✅ |

### L6 厂商开源栈（驱动/HAL 源码）
| 资料 | 来源 | 本地落点 | 状态 |
|---|---|---|---|
| CLO taro vendor 平台（LA.VENDOR…QSSI13.0） | `lineage-ovaltine-dev/clo_platform_vendor_qcom_taro` | `sources/lineageos/clo_platform_vendor_qcom_taro` | ✅ |
| camera-kernel（CAMX 内核侧） | `lineage-ovaltine-dev/android_vendor_qcom_opensource_camera-kernel` | `sources/lineageos/vendor_qcom_opensource_camera-kernel` | ✅ |
| hardware/oplus 之外的 QCOM CAF（audio/display/media/power）、`vendor/qcom/opensource/*` | LineageOS 官方 manifest 自动拉取 | — | ❌ 属 L8 平台源码，勿单独手拉 |

### L7 Recovery / 刷入链路
| 资料 | 来源 | 本地落点 | 状态 |
|---|---|---|---|
| OrangeFox/PBRP 设备树（fox_12.1、fox_14.1） | `QQiuAaaa/ofrp_device_oneplus_ovaltine` | `sources/community/orangefox_device_ovaltine` | ⚠️ 面向 10T，需核 PGP110 |
| PGP110 TWRP（android-12，含 prebuilt kernel/dtbo） | `Abandon-Project/twrp_device_oneplus_PGP110` | `sources/community/abandonproject_twrp_PGP110` | ✅ |
| 分区布局：super 11,274,289,152B / dynamic 5,637,144,572B，A/B + Virtual A/B，`--flags 3`（verity+verification 关闭） | `BoardConfig.mk` / `BoardConfigCommon.mk` | 已落盘 | ✅ |
| 固件刷入顺序（25 个 `--slot=all` 命令） | `lineage-ovaltine-dev/android_manifest` README | `sources/lineageos/manifest_ovaltine` | ✅ |

### L8 LineageOS 平台与构建
| 资料 | 来源 | 状态 |
|---|---|---|
| 官方 manifest（`repo init -u https://github.com/LineageOS/android.git -b lineage-23.2 --git-lfs`） | LineageOS | ❌ 平台源码 150-200GB，只能在 Linux 机 sync |
| 社区 local manifest | `lineage-ovaltine-dev/android_manifest` 只到 lineage-21 | ⚠️ 已按 23.2 各仓库真实分支重写：`local_manifests/ovaltine.xml` |
| 构建主机要求 | LineageOS 官方构建文档模板（Ubuntu 22.04/24.04 x86_64、区分大小写文件系统、≥150GB 空闲、JDK、python3、repo、git-lfs、ccache） | ❌ 本机 Windows 不满足 |
| vendor/extra、MindTheGapps | `Luk1337/android_vendor_extra`、`MindTheGapps/vendor_gapps` | ❌ 由 manifest 拉，不手搬（GApps 亦可用 `-Gapps` 移植包代替） |
| 签名密钥（releasekey/platform） | 自建 | ❌ 必做，否则无 OTA、无 verity |

### L9 验证与回归资料
| 资料 | 来源 | 状态 |
|---|---|---|
| VINTF（manifest/compatibility_matrix/framework_compatibility_matrix、odm 版） | 设备树 + sm8450-common | ✅ 已落盘 |
| 已知问题/历史改动流水（含跨机型借 blob 记录） | `sources/lineageos/release_ovaltine/CHANGELOG*.MD` | ✅ |
| 回归项定义（本文第五节） | — | ✅ |

---

## 三、缺口台账：缺什么 → 后果 → 补救

按严重度排序。**硬缺口**=必须自己去拿，GitHub 拿不到；**软缺口**=可用同平台/同 SoC 资料替代或补齐。

| # | 缺口 | 类型 | 后果 | 补救（按成本从低到高） |
|---|---|---|---|---|
| G1 | ~~PGP110 官方全量底包~~ **→ 已到位（2026-09-25）**：`PGP110_15.0.0.1901(CN01)`（7.22GB，payload.bin 未加密）+ `CPH2415_15.0.0.1901(EX01)`（6.50GB），均在项目根目录；且社区 `vendor/oneplus/ovaltine` 自带 `radio/` **24 个固件镜像（360MB）**，其 `abl/xbl/xbl_config` 的 SHA1 与设备树 `proprietary-firmware.txt` 的 pin **逐一实测吻合** | 残留：软 | 底包本体不再阻塞；剩余唯一风险是 bootloader 版本选择（见 G15） | 已用本机编译的 `payload-dumper-go -l` 实测：两包各 46 分区、集合完全一致、24 个固件镜像全在（modem 285MB / odm 1.3GB / vendor 677MB）→ 详见 8.1 |
| G2 | **BL 解锁资格**（国行深度测试） | 硬 | 不解锁 = 一切免谈；解锁后 AVB 失效、部分安全特性降级 | 社区申请流程；解锁前 `getprop ro.boot.slot_suffix`/反回滚版本备份；`oplusstanvbk` 反回滚索引高版本刷低版本会被拒 → 全程只升不降 |
| G3 | **LineageOS/AOSP 平台源码**（150-200GB） | 硬（本次刻意不拉） | 无法编译；也无法跑 `breakfast ovaltine` 校验依赖 | Linux 构建机上 `repo init -b lineage-23.2` + 本仓库给的 `local_manifests/ovaltine.xml`；本目录当资料仓，勿在本机 sync |
| G4 | **lineage-22/23 社区 manifest 缺失**（官方只发到 21） | 软 | 按 README 走只能构建 lineage-21（Android 14），拿不到 23.2 的高版本与修 | 已生成 `local_manifests/ovaltine.xml`（按各仓库实际存在的 23.2 分支拼）；首次 `repo sync` 后需核对 `hardware/qcom-caf/sm8450` 等子仓库分支 |
| G5 | **设备树身份串写死海外版**：`PRODUCT_MODEL=CPH2415`、`BuildFingerprint=OnePlus/CPH2415/OP5552L1:15/...`、`vendor.prop` 名称 "OnePlus 10T 5G"。**板级校验不是问题**——实测两底包 metadata：`PGP110 → pre-device=OP5551L1`、`CPH2415 → OP5552L1`，而 `board-info.txt` 已同时列 `taro|OP5551L1|OP5552L1`，即社区树本来就接受 Ace Pro | 软 | 蓝牙/Wi-Fi 直显名、OTA 与 App 机型判定、DRM/服务侧看到的是 10T 而非 Ace Pro；观感与统计口径问题，不影响功能 | 3 处改动：`lineage_ovaltine.mk` 的 `PRODUCT_MODEL`/`BuildFingerprint`/`DeviceName`/`DeviceProduct`、`vendor.prop` 的 `bluetooth.device.default_name` 与 `vendor.usb.product_string`、（可选）`overlay/` 的 power_profile/dimens |
| G15 | **bootloader 固件版本被刻意 pin 在 OOS 13**，且不可随意升级：提交记录 `ovaltine: Pin OOS 13 xbl — newer ones killed EDL access`（2025-12-22）、`ovaltine: Pin OOS 13 abl — For some users, bootloader mode crashes with OOS 14 abl`（2025-01-06） | 硬（不可逆风险） | 刷 15.0.0.1901 包里的 `xbl/xbl_config` → **可能永久失去 9008/EDL 深刷自救通道**；刷 OOS14+ `abl` → 部分机器进 fastboot 直接崩 | **不要**从新包刷 `abl/xbl/xbl_config`（保留 `radio/` 里 sha1 已核对的 OOS13 版本）；其余固件（modem/dsp/tz/aop…）可用 PGP110 15.0.0.1901 的。刷机前务必先拿到 PGP110 的 **MSM 深度刷机包**做最后兜底 |
| G6 | blob 缺 2 条（`odm/lib64/librfapis.so`；`libRGBC.so` 路径 vendor↔odm） | 软 | 个别场景：RF 相关属性读取失败、色彩增强(rgbc)库缺失；`BoardConfigVendor.mk` 找不到文件时打包报错 | 直接从已落盘的 `gitlab_ovaltine@lineage-22.2` 或 `amitkumar2007@lineage-23.1` 拷贝；路径差异按 `proprietary-files.txt` 的 `|rename`/`SYMLINK` 语法处理 |
| G7 | ~~相机逐模组校准~~ → **已排除**：同代官方 `camera-devicetree` 两分支 **0 差异**（8.5），主摄/超广/微距/前摄同一套；blob 里含 `sensormodule.qtech_imx766_main.bin` | 无 | 无 | 仍建议在第 2 步 diff 两包 `odm/lib64/camera/` 的文件清单做一次确认（原料已在本地底包内） |
| G8 | **150W SUPERVOOC 充电/电量计**（`oplus_chg` + odm `firmware/fastchg/*` + dts `sc857*`）→ 已定位到**唯一差异点**：官方 A15 两分支的 `oplus-chg-21841.dtsi` 仅差 1 行 `oplus,full_limit_curr_support;` | 软（极小） | 若属性不匹配：限流策略偏保守（快充功率上限）或电流上报异常 | 编国行版时把这 1 行加/删进 `kernel/oneplus/sm8450-devicetrees/oplus/oplus_chg/oplus-chg-21841.dtsi` 并实测功率；其余充电配置两侧完全一致 |
| G9 | 高刷/AOD/HBM 显示策略 → 面板 dts **两分支完全一致**（`dsi-panel-oplus-ovaltine_boe_nt37705_1080_2412_dsc_cmd.dtsi`），display 差异属其他机型 | 软（仅调优层面） | 无 120/144Hz、亮度阶跃、AOD 异常属移植调优问题，不是国行/国际差异 | 用 `device/oneplus/ovaltine/configs/display_id_*.xml` + 实验分支（`lineage-22.1-displayconfig`、`lineage-21-touch`、`lineage-21-zram`）逐个试 |
| G14 | 国行独有节点的 **SELinux 标签/策略**（`device/qcom/sepolicy_vndr` 未随本目录落盘，`hardware/oplus/sepolicy/qti` 已落盘） | 软 | 新节点（充电/BMS/显示 HBM）无 file_contexts/genfs_contexts → HAL 被 deny、静默失效，日志只有 avc | 在构建机上改 `device/qcom/sepolicy_vndr` 或 oplus sepolicy；刷机后 `dmesg | grep avc` + `adb shell audit2allow` 逐条收；这是移植最常见的实际工作量所在 |
| G10 | **AVB/签名与 Widevine L1** | 软 | 用 AOSP testkey → 无 OTA 校验、不能签 Recovery/Update；BL 解锁后 Keymaster/TEE 降级 → **Widevine L1→L3**（Netflix 等掉 720p） | 自建 releasekey 并备份；L1 无法保证 → 若用户强依赖 L1，方案是保留原厂不解锁（属需求取舍，不是技术问题） |
| G11 | **构建主机缺失**：本机是 Windows（NTFS 保留名实测令 10 个内核树 checkout 失败，靠 `core.protectNTFS=false` 绕过；大小写不敏感）、**未装 WSL**（`wsl --status` 明示未安装）、D 盘仅剩 48GB（底包占 13.7GB） | 硬 | 本机既不能编 ROM，也读不了底包里的 ext4 内容 → blob diff 这一步也做不了 | 分两档，见 8.7：① `wsl --install` 后即可解包+diff（约 15GB 临时空间）；② 编译需 150-200GB/16-32GB 内存的 Linux（另一台机、外接 SSD，或注册为 self-hosted runner）。搬这批资料用裸仓/`git bundle`，别拷工作区 |
| G12 | 官方 blob 通路（TheMuppets/LineageOS 均空占位） | 软 | 不能按 LineageOS 官方 wiki 流程 `extract-files.sh` 直连官方 blob 服务器 | 用社区 GitLab blob（已落盘）；或完全自提取（extract-utils 已落盘，`./extract-files.py` 指向解包后的底包目录） |
| G13 | 国行专属子系统：eSIM（10T 海外版有、国行无/反之）、VoNR/SA 运营商配置、`my_region`/`my_bigball` 分区里的区域资源、NFC SN100 支付 | 软 | 通话/VoWiFi/载聚合异常、NFC 刷卡不可用、区域字体/服务缺失 | 底包提取时把 `my_*` 分区的 build.prop 一并纳入 diff（`dump_CPH2417/my_region|my_bigball|my_product/build.prop` 已落盘可先比对）；modem 固件**必须**用 PGP110 版本，不要混用海外 modem.img |

---

## 四、"同处理器资料能否借用"判断表

| 来源机型 | 与本机型关系 | 可借 | 不可借 | 本次是否落盘 |
|---|---|---|---|---|
| **OnePlus 10T（CPH2415/CPH2417）** | 同代号 ovaltine、同主板家族 | 几乎全部：dts/dtso、blob、固件清单、设备树、recovery、移植包 | 运营商/区域配置、modem（频段）、机型身份串、部分校准 bin | ✅ 全套 |
| **OnePlus 10 Pro `kebab`（SM8450，官方支持至 lineage-24.0）** | 同 sm8450-common 血统、LineageOS 官方维护 | **架构模板**：sepolicy、init.rc、VINTF、Makefile 组织、内核 GKI 做法、升级到新 LineageOS 分支的迁移路径 | 相机/显示/音频/传感器 blob（不同机型） | ✅ 树 12 分支 |
| **OnePlus 11R / Ace 2 `udon`（同 SM8475）** | 同 SoC 不同机型 | 内核/CLO/DLKM/CAMX 结构、部分 QCOM HAL 版本对齐 | **所有 vendor blob**：面板、摄像头模组、触控、指纹、充电、音频功放全不同 | ✅ 仅 vendor（803MB）作对照 |
| **Motorola SM8475（LineageOS 官方）** | 同 SoC 他厂 | GKI 5.10 + DTBO/DLKM 的工程组织方式（官方维护范例） | 一切 oplus 专有 | ❌ 需要时按需查 |
| **Nothing Phone (2) SM8475（LineageOS 官方 kernel）** | 同 SoC 他厂 | 内核上游化/GKI 补丁思路 | 驱动细节、blob | ❌ |

判断原则：**内核/构建框架可按 SoC 借，blob 与硬件描述文件（dts/dtbo/校准 bin/固件）只认同机型。** 本次已落盘的 PGP110 dtb/dtbo 正是"认同机型"的那一份，别用 10T 的替换它。

---

## 五、执行顺序（依赖已排好）

1. 记录现状：`getprop | grep -E "board|model|slot|vr_|bootdevice"`、反回滚版本、当前 ColorOS 版本号；**PGP110 的 MSM 深度刷机包必须拿到**（唯一救砖通道，见 G15）。
2. 解两份底包（**在 Linux 上做**：`-l` 列分区表已在 8.1 完成，但 ext4 内容 Windows 读不了）：`payload-dumper-go -p odm,vendor,vendor_dlkm,product,system,system_ext <zip>` 分别解出国行与国际分区镜像，`lpunpack`/直接挂载后做文件清单 diff → 一次列出国行独有 blob。
3. 申请/完成 BL 解锁；备份 `/odm`、`/vendor`、`abl`/`xbl`/`xbl_config`/`oplusstanvbk`（`dd if=/dev/block/by-name/...`，这是回退 bootloader 的唯一凭据）。
4. Linux 构建机（Ubuntu 22.04/24.04，≥150GB，大小写敏感）：`repo init -u https://github.com/LineageOS/android.git -b lineage-21 --git-lfs`（先取有成品佐证的分支），放 `local_manifests/ovaltine.xml`（本仓库已生成，按目标分支改 revision），`repo sync -c`，`repo forall -c 'git lfs pull'`。
5. 第一轮**先不碰 blob**：直接用社区 `vendor/oneplus/*`（含 `radio/` 固件）把 `lineage_ovaltine` 编出来并刷通，作为"基线是否健康"的判定。
6. 第二轮再换成第 2 步自提的 PGP110 blob：跑 `device/oneplus/ovaltine/extract-files.py`（其 `ExtractUtilsModule` 带 `add_firmware_proprietary_file=True`，会连同 `proprietary-firmware.txt` 列的固件一起抽；解包目录参数看 `--help`），产出 `vendor/oneplus/{ovaltine,sm8450-common}`，与社区 blob diff。**固件只刷 modem/dsp/aop/tz/oplus_sec/qupfw/shrm 等区域相关项，`abl`/`xbl`/`xbl_config` 保持 `radio/` 里的 OOS13 pin 版（`bash .fetch/verify.sh` 可复核这三个 SHA1）。**
7. 改 3 处身份串为 PGP110（`lineage_ovaltine.mk`、`vendor.prop`、可选 overlay）；板级校验无需动——`board-info.txt` 已含 `OP5551L1`。
8. 回归测试（下一节），把结果与 blob diff 清单反馈给 `lineage-ovaltine-dev`（他们只接过 10T）。

**回归项**：主摄/超广/微距/前摄（含 OIS、录像 4K、HDR）、屏下指纹注册与解锁、120/144Hz 与 AOD、自动亮度、150W 超级快充识别与功率、扬声器/听筒/录音/杜比、WiFi 2.4/5/6 + 蓝牙 + NFC + GPS 锁星、VoNR/双卡/载聚合、通话降噪、USB 文件传输与 3.5mm? (无) 、传感器（光线/距离/霍尔）、省电待机唤醒、EdXposed/Root 无关项跳过、Widevine 级别、恢复出厂后首启、OTA（自建签名）。

---

## 六、本次已落盘统计

- **40 个仓库**，`sources/` 实测 **20,441MB**；两份底包 13.7GB 也落在本目录 → **D 盘仅剩 48GB（94% 已用）**。
- 分类体量：`sources/kernel` 11,675MB（12 个内核/dtso 树）· `sources/community` 3,780MB（其他维护者设备树 + recovery + 对照 blob）· `sources/vendor` 3,429MB（GitLab blob 全分支）· `sources/lineageos` 168MB（移植设备树/HAL/工具）· `sources/tools` 1MB（payload-dumper-go、ozip 解密）。
- 拉取脚本与日志：`.fetch/pull.sh`、`.fetch/pull3.sh`、`.fetch/pull.log`、`.fetch/checkout.log`、`.fetch/k23.log`；blob 差集：`.fetch/need2.txt`、`.fetch/have.txt`、`.fetch/missing2.txt`。
- 复现/补拉：`bash .fetch/pull.sh`（幂等，已存在则 SKIP）。自检：`bash .fetch/verify.sh` → 校验 `radio/` 里 abl/xbl/xbl_config 的 SHA1 是否等于设备树 pin、blob 缺口数、两份底包是否在位（2026-09-25 实跑：3/3 OK、1543 需要/2 缺失、两包在位）。
- 注意事项：GitHub 必须走 SSH（本机 `https://github.com` 被 Windows Schannel 吊销检查拦死，`gitlab.com` HTTPS 可用）；内核树检出需 `git config core.protectNTFS false`。

## 七、明确没拉的东西及原因

| 项 | 原因 |
|---|---|
| LineageOS/AOSP 平台源码（`LineageOS/android` 全 manifest） | 150-200GB，Windows 上既不能构建也没地方放；到 Linux 机 `repo sync` 即可，本目录只需 manifest |
| `MindTheGapps/vendor_gapps`、`Luk1337/android_vendor_extra` | 由 manifest 自动拉；移植包已有 `-Gapps` 变体可不拉 |
| QCOM CAF/HAL 全量（`hardware/qcom-caf/sm8450/*`、`vendor/qcom/opensource/*`） | 同上，属平台依赖，单独手拉会造成版本错配 |
| PGP110 **MSM 深度刷机包** | OEM 分发渠道，GitHub/GitLab 无合法镜像；两份 OTA 已到位，只缺这个救砖兜底 → G15 |
| `OnePlusOSS/android_kernel_platform_msm-kernel`、`android_kernel_qcom_sm8450`、`android_kernel_oneplus_sm8450-oos`（各 1.4-2.6GB） | 与已落盘的 lod 内核/官方分支重叠，构建时不需要；要查历史再补 |
| `lineage-ovaltine-dev/lineage_wiki`（fork） | 只是 wiki fork，官方 wiki 已按需读取 |

## 八、2026-09-25 追加判定（两份底包到位后）

实测两份底包 metadata：`PGP110_15.0.0.1901(CN01)`（Android 15，`pre-device=OP5551L1`，patch 2026-07-01，7.22GB）与 `CPH2415_15.0.0.1901(EX01)`（`pre-device=OP5552L1`，patch 2026-06-01，6.50GB）。两者都是**未加密标准 A/B OTA**，`payload.bin` 可被 payload-dumper-go 直接解。

用本机现编的 `payload-dumper-go -l` 实测两包分区表：**各 46 个分区，两边集合完全一致**（`comm` 差集为空），且设备树 `proprietary-firmware.txt` 要的 24 个固件镜像**全在**（abl 221KB / aop / aop_config / bluetooth / cpucp / devcfg / dsp 67MB / engineering_cdt / featenabler / hyp / imagefv / keymaster / **modem 285MB** / oplus_sec / oplusstanvbk / qupfw / shrm / splash / tz / uefi / uefisecapp / xbl / xbl_config / xbl_ramdump），另有 `vendor 677MB`、`odm 1.3GB`、`vendor_dlkm 159MB`、`my_region`/`my_carrier`/`my_bigball`/`my_stock` 等国行区域分区 → **提 blob 与提固件的原料齐了，G1 关闭**。

### 8.1 底包用国行还是国际版 → **以国行 PGP110 为主源，bootloader 三件套谁都不用**

| 用途 | 取哪个 | 理由 |
|---|---|---|
| `extract-files.py` 提 vendor/odm blob | **PGP110** | 同机同源；校准 bin、`my_region`/carrier、国行属性只在 CN 包里对得上 |
| modem/dsp/aop/tz/oplus_sec/qupfw/shrm/oplusstanvbk 等固件 | **PGP110** | 国行频段（n28/n79/n41 等）与 NV 配置；刷国际 modem 会掉频段甚至拒刷 |
| `abl` / `xbl` / `xbl_config` | **都不刷**，用社区 `radio/` 里 SHA1 已核对的 OOS13 版 | 见 G15：新 xbl 会**打死 EDL 深刷通道**（不可逆），OOS14 abl 有 fastboot 崩溃报告 |
| 找"国行独有/缺失 blob"的 diff 基准 | **两个都解** | 一次 diff 列出差异清单，比事后逐个猜省几天 |

铁律：**固件与 ROM 内 blob 必须同版本**（VINTF/HAL 配对）。所以第一轮直接全用社区现成的（`radio/` 固件 + 23.2 blob，同属 15.0.0.1601 系）先把机器点亮；第二轮再换成自提的 1901 blob 做增量 diff。不要一轮里混两个版本。

### 8.2 GitHub Actions 编译 → 能，但必须 self-hosted

- GitHub 托管 runner（ubuntu-24.04 标准档）**只有 4 核 / 16GB 内存 / 150GB 磁盘、单 job 上限 6h**：LineageOS 全量 `repo sync` 就要 100GB+，首次编译（GKI 内核 + DLKM + 系统镜像）在 4 核上极易爆内存或超时；larger-runner 档位要付费额度，性价比差。
- 正确形态：**你自己的 Linux 机跑 self-hosted runner**（≥8-16 核、32-64GB、≥400GB），Actions 只负责编排 + 缓存（repo mirror / ccache）+ 发 Release。
- 要提交的仓库：一个 manifest 仓（`.github/workflows/build.yml` + `local_manifests/ovaltine.xml` + 你自己的 patch 目录）+ fork 到个人 org 的设备树与内核树（ovaltine、sm8450-common、kernel×3）。
- **不要提交**：自提的 PGP110 vendor blob（放 **private** 仓或只当 artifact——把 OEM 闭源 blob 公开推 GitHub 是 DMCA 风险，社区那份 GitLab 本身就在灰区）、stock OTA zip 本体。
- 签名密钥一次性生成并存 GitHub Secrets（base64 打包 `build/target/product/security/*`）；换 key = 之后 OTA 链全断。

### 8.3 社区版现状与"为什么没被官方收编"

- 完成度：**设备树/公共树源码已到 lineage-23.2**（最后提交 2025-12-28 / 2026-01-31），但 `release` 仓库**公开发布只到 lineage-21（2024-10-03）**，共 9 个 release → 22.x/23.2 属"源码可用、无成品包"，想上高版本只能自己编。
- 不是技术问题：主力贡献者就是 LineageOS 核心成员（短 log 统计 LuK1337 57 / Bruno Martins 33 / pppig236·Weikai Kong 50 / AdarshGrewal 9），提交里有 `Implement DT2W via power HAL extension`、`Migrate to AIDL LiveDisplay HAL` 这类上游级实现。
- 没进官方的实际原因（**从仓库形态推的判断，LineageOS 无公开拒绝记录**）：① blob 通路不合规矩（个人 GitLab + 跨机型 Kang：salami/dodge/ingot/8T，官方要求进 TheMuppets 且来源可追溯）；② 构建依赖 out-of-tree cherry-pick（README 至今要求 `repopick -t 13-ovaltine`、`repopick -p 342864`，官方不收需要 repopick 的机型）；③ 收编=长期多养一个 sm8450-common fork（官方 sm8450 只有 kebab/10 Pro）；④ `LineageOS/android_device_oneplus_ovaltine` 建仓却是**空仓库** → 收录流程开过头就停了。
- 当前明确的功能取舍（提交史实证，可当"已知不足"清单）：`Remove oplus performance HAL` → `Kang oplus perf from salami`（性能/调度换成别机型的）、`Disable LSM audio feature`、`Disable LiveDisplay SunlightEnhancement`、`Disable UFFD GC`、`Rip esim kmod`、`Rip feedback modules`、`Deny access to HBM node`（与屏下指纹调亮冲突）、`Kang auto brightness configs from OnePlus 8T`、WFD 投屏 blob 加了又 `Revert`、`Avoid building OPLUS doze` 与 `Switch to OPLUS sensors AIDL` 均被 `Revert`。

### 8.4 基于社区版，还是只参考 → **基于**

从零"参考着做"等于重做它 3 年踩完的坑（5.10 GKI + oplus odm/CAMX 依赖矩阵 + AVB/Virtual-A/B 布局 + 一堆 ELF fixup）。你要承担的是它的 **PGP110 变体**，只有 4 件事：① 自提 PGP110 blob 并 diff；② 身份串（G5 的 3 处）；③ 国行 modem/区域/运营商回归；④（可选）向 lineage-24.0 迁移。
版本路线：**先编 lineage-21**（唯一有正式发布与实机反馈的分支，出问题时能区分"你改坏了"还是"移植本来没做完"）→ 通过后再上 22.2 → 23.2。

### 8.5 官方 dts 实测差异：Ace Pro ↔ 10T 在同代（A15）源码里几乎完全相同

对比 OnePlusOSS 同代两分支 `oneplus/sm8475_v_15.0.0_ace_pro` 与 `oneplus/sm8475_v_15.0.0_oneplus_10t`（各 ~1405/1408 个 dts 文件，落在 `sources/kernel/dt15_ace_pro`、`dt15_oneplus_10t`）：

| 子系统 devicetree | 差异 | 与 ovaltine 相关？ |
|---|---|---|
| `camera-devicetree` | **0** | 相机 dts 完全一致 → G7 的"模组供应商差异"风险实测排除 |
| `audio-devicetree` | **0** | 一致 |
| `display-devicetree` | 5 个文件 | **无关**（是 1440×3216 三星 amb670yf08 等其他机型面板；ovaltine 的 NT37705/r66451 面板 dts 两侧一致）→ G9 排除 |
| `devicetree/oplus` | 4 个文件，全在 `oplus_chg/` | ovaltine 项目号 **21841 只差 1 行**：10T 侧多 `oplus,full_limit_curr_support;`；`21845` 侧 **0 差异**；另 22015/22803 是别的机型 |

结论：**移植在硬件描述层面无需为 Ace Pro 新增任何 dts**；社区 `kernel/oneplus/sm8450-devicetrees` 本来就带着 `ovaltine-21841-cape-overlay.dts`、`ovaltine-21845-cape-overlay.dts`、`oplus-chg-21841.dtsi`、`ovaltine-nfc-sn110t.dtsi`，内核 defconfig `arch/arm64/configs/vendor/oplus/ovaltine.config` 也在。唯一可能要动的是给 `oplus-chg-21841.dtsi` 补那 1 行属性（G8 从"未知风险"降级为"一行改动 + 实测确认"）。
项目号对照：OTA metadata 里 `PGP110 → oplus_project_numbers=21841`、`CPH2415 → 21842`，而官方 dts 只用 `21841`/`21845` 两个 ovaltine overlay 文件——说明国行/国际靠 **board id 选 overlay**，不是靠不同 dts。

### 8.6 能做到"零硬件功能损耗"吗 → 不能，上限是"日常无感"

| 子系统 | 预期 | 依据/风险 |
|---|---|---|
| 蜂窝（双卡/VoNR/载聚合/国行频段） | 基本无损耗 | PGP110 modem + 社区树自带 `qcril-database`；需逐运营商实测 |
| 相机主链路（多摄/OIS/4K） | 大概率对齐 | blob 与 dts 同源、主摄同 IMX766；**OPPO 影像算法链**（AI 场景、超清、杜比视界录制）不保证 |
| 150W 超级闪充 | 可能保得住，须实测 | odm `firmware/fastchg/*`、`smart_chg_config_table.txt`、dts `sc857*` 都在；充电 UI/智能充电策略与原厂不同 |
| 屏下指纹 / 高刷 / AOD / 双击亮屏 | 已实现 | `Deny HBM node`、UDFPS dimming、`Implement DT2W via power HAL extension`、`display_id_*.xml` |
| 蓝牙音频编解码 | 通常保留 | 树里有 aptX / aptX HD / aptX Adaptive / LDAC 配置痕迹 |
| NFC | 读写与支付可用，**厂商 eSE 服务丢失** | SN100 在；门禁卡/交通卡迁移与一加钱包类服务无对应组件 |
| 杜比全景声 | 可能缺 | 社区 ovaltine 树未见 dolby 配置，同 SoC 的 udon 树有 `configs/dolby` |
| Widevine | **L1→L3，不可逆** | BL 解锁即触发，与 ROM 无关 |
| 保修 / 风控类 App | 受影响 | 银行/游戏风控检测 `verifiedbootstate=orange` |

一句话：**"零损耗"在解锁 BL 那一刻就不成立了**；以现在这套资料 + 社区 23.2 的完成度，现实目标是"通话、流量、相机主链路、指纹、高刷、NFC、快充全活"，剩下的是逐台实测的回归工，不是技术障碍。

### 8.7 构建主机判定（本机实测：无 WSL、D 盘剩 48GB）

`wsl --status` → **"未安装适用于 Linux 的 Windows 子系统"**；`sources/` 20.4GB + 底包 13.7GB 已把 D 盘吃到 48GB 空闲。按任务量分档：

| 要干的事 | 需要 | 本机现状 | 结论 |
|---|---|---|---|
| 解 `payload.bin` → 读 odm/vendor 内容 → 与社区 blob 做清单 diff | ~15GB 临时 + ext4 读取（WSL2 或任意 Linux） | 无 WSL；`payload-dumper-go` 已能跑但读不了 ext4 | **缺一个 Linux 环境**，`wsl --install`（需管理员+重启+虚拟化开启）即可，成本最低 |
| 编 ROM（repo sync + 首次全量编译） | 150-200GB 磁盘、16-32GB 内存、大小写敏感 fs | 48GB 空闲、无 Linux | **本机不可行**，需要另找/扩容：外接 SSD + WSL2、旧机装 Ubuntu、或云主机/self-hosted runner |
| 只做 Actions 编排 | 一个能跑的 runner | — | 见 8.2：把上面那台 Linux 机注册成 self-hosted runner |

→ 所以顺序是：**先装 WSL2 把 blob diff 做掉**（产出"国行独有 blob 清单"，这是唯一还需设备/底包侧信息的未知项），再决定编译放哪台机器。别反过来先折腾 CI。

## 九、国行底包实测结果（2026-09-25，在手机 Ubuntu 容器里解包）

工具链：Termux → `proot-distro install docker.1ms.run/library/ubuntu:24.04` → 容器内 `payload-dumper-go`
（go1.22 现编，需 `liblzma-dev` + `GOPROXY=https://goproxy.cn`）+ `erofs-utils`。镜像 7.22GB 已传到手机
`~/dl/PGP110_cn.zip`，解出 24 个固件 + 7 个系统分区（6.5GB），清单在 `.fetch/cn-files.txt`、`.fetch/fw.sha1`。

| 实测项 | 结果 | 置信度 |
|---|---|---|
| 分区覆盖 | payload 内 46 个分区；设备树 `proprietary-firmware.txt` 要的 **24 个固件镜像 100% 齐备**（`comm` 双向差集为空，含 modem 285MB / dsp 67MB / tz 3.8MB） | A 实测 |
| 文件系统类型 | odm/vendor/product/system/system_ext/dlkm 全是 **EROFS**（不是 ext4）→ 必须用 `fsck.erofs --extract`，e2fsprogs 无用 | A 实测 |
| blob 覆盖 | 设备树 `proprietary-files.txt` 需要 1543 条，国行 stock 命中 **1542 条**；唯一缺 `odm/etc/vintf/manifest/manifest_oplus_fingerprint_aidl_v2.xml` | A 实测 |
| 之前判定的 2 个"缺 blob" | `librfapis.so`、`libRGBC.so` 在国行 stock **都存在**（lib 与 lib64 各一份）→ 不是缺口，只是社区 23.2 blob 树没同步 | A 实测 |
| **指纹栈差异（唯一的实质硬件差异）** | 国行是 `manifest_oplus_fingerprint_aidl.xml`（无 `_v2`）；`fingerprint@2.1-service_uff` 哈希 `1ea1131b…` ≠ 社区 pin 的 `0ded3621…`；同目录下 `.rc` 与 `sensors.ssc.so` 哈希一致 | A 实测 |
| **bootloader 固件差异（G15 得证）** | 国行 15.0 的 `abl=16bcfa80…`、`xbl=3eae9952…`、`xbl_config=3da9823b…` 与 pin 的 OOS13 版**全部不同** → 整包刷会换上"会打死 EDL"的新 xbl | A 实测 |

### 9.1 逐文件哈希实测：2599 条需求里 2316 条与国行完全一致

把两棵设备树要求的 blob 合并去重得 **2599 条**，与社区 blob 树（`lineage-23.2`）和国行 stock（PGP110 15.0.0.1901）做三方比对（脚本 `.fetch/hashcmp.sh`，产物 `.fetch/out-*.txt`）：

| 分类 | 数量 | 说明 |
|---|---|---|
| 内容完全一致 | **2316** | 绝大多数栈相同 |
| 内容不同（同路径不同哈希） | **266** | 集中在区域/硬件相关项：`vendor/lib64/camera/` 17、`odm/etc/wifi/` 8（`bdwlan` 每区域不同）、`vendor/lib64/egl/` 6、`odm/etc/camera/` 5、`vendor/lib64/hw/` 4、`odm/lib64/camera/` 4，余为零散 |
| 只在社区树里 | **8** | 全部是**较新的高通定位栈**（`qcc-trd`、`libloc_base_util/mq_client/qwes_iface/ril_client`、`liblowi_common`、`vendor.qti.gnss-V5-ndk.so`） |
| 只在国行 stock 里 | **9** | 含 `odm/etc/vintf/manifest/manifest_oplus_fingerprint_aidl.xml`（社区用的是 `_v2` 版） |
| 两边都无 | 0 | 需求全部可从"社区+国行"并集满足 |

### 9.1b 由此修正策略：默认自提国行，只有定位栈沿用社区

| 采纳 | 理由 |
|---|---|
| 266 条内容不同的 → **用国行版** | 相机 sensormodule 校准、`bdwlan` WiFi 板卡配置、EGL/GPU 驱动都是**区域与硬件相关**，套 10T/借自 salami 的版本是错配来源，不是省事 |
| GNSS 那一簇（8 条 + `vendor.qti.gnss-service.so` + 相关 `libloc_*`） → **整簇沿用社区混源版** | 国行只到 `gnss-V1/V3-ndk_platform`，社区树已从 NE2211/NE2311 拿到 V5；混用两个版本才是真风险，所以整簇同源替换，实测定 GPS |
| `manifest_oplus_fingerprint_aidl.xml` → 用国行版 | 国行没有 `_v2` 那条；已在 `patches/device_oneplus_ovaltine.patch` 处理（并去掉借自 salami 的哈希钉） |

（本节前一轮的结论"不要整体自提"是**过头了**：按实测，自提 2591 条 + 保留社区定位簇才是正确做法。）



1. **固件混刷**，已固化为 `flash-firmware.sh`：21 个镜像用国行 15.0，`abl/xbl/xbl_config` 用社区 `radio/` 的 OOS13 版，脚本先校验三个 pin 哈希再刷，不符即中止。
2. **指纹**：`proprietary-files.txt` 里那行的注释是 **`# Fingerprint (service) - from salami 11.F.68`** —— 社区的一加指纹 HAL 二进制是从 **OnePlus 11(salami)** 借来的，这解释了国行哈希为何不同（`1ea1131b…` vs pin 的 `0ded3621…`）；同时国行的 AIDL manifest 叫 `manifest_oplus_fingerprint_aidl.xml`（无 `_v2`）。
   已固化为 `patches/device_oneplus_ovaltine.patch`（本地 `git apply --check` 通过），内容三处：PGP110/OP5551L1 身份串、蓝牙与 USB 显示名、指纹条目换成国行文件名并**去掉哈希钉**（让 extract 接受国行二进制）。
   用法：先用 `extract-files.py` 指向 `/dl/cn/x` 自提国行 blob，再在 Actions 里勾 `use_cn_patches`。第一轮**不要**勾（社区 blob 树里没有国行那个文件名，勾了会构建失败）。
3. 其余 1542 条 blob 与 10T 完全同源 → **相机/显示/触控/充电/传感器/NFC 都不需要为国行改动**（与 8.5 的 dts 差异结论互相印证：dts 侧只差充电里 1 行属性）。

### 9.3 手机侧环境事实（复现要用）

- 容器：`proot-distro install docker.1ms.run/library/ubuntu:24.04`（Docker Hub 直连被墙）。
- 基础镜像**没有 ca-certificates** → apt 全源 HTTPS 握手失败；解决：把 Termux 的 `cert.pem` bind 成 `/etc/ssl/certs/ca-certificates.crt`（见手机上的 `~/u.sh`）。
- apt 源换 `mirrors.aliyun.com/ubuntu-ports`；Go 模块走 `GOPROXY=https://goproxy.cn`；`payload-dumper-go` 编译需 `liblzma-dev`，其 HEAD 需要 go≥1.27（容器只有 1.22）→ 用 tag `1.3.0`。
- 编译 ROM **不能**在这台机器上做：arm64 主机没有 AOSP host 预编译（见 8.2/README 硬约束）。

---

## 来源

- [GitHub 托管运行器参考（runner 规格）](https://docs.github.com/cn/actions/reference/specifications-for-github-hosted-runners) · [GitHub 托管的运行程序](https://docs.github.com/zh/actions/using-github-hosted-runners/using-github-hosted-runners/about-github-hosted-runners)
- [KHwang9883/MobileModels — 一加型号-代号对照](https://github.com/KHwang9883/MobileModels/blob/master/brands/oneplus.md)
- [lineage-ovaltine-dev（社区移植组织：设备树/内核/blob/manifest/release）](https://github.com/lineage-ovaltine-dev)
- [GitLab：lineage-ovaltine-dev/proprietary_vendor_oneplus](https://gitlab.com/lineage-ovaltine-dev/proprietary_vendor_oneplus)
- [OnePlusOSS/android_kernel_oneplus_sm8475（含 ace_pro / 10t 官方内核分支）](https://github.com/OnePlusOSS/android_kernel_oneplus_sm8475)
- [LineageOS/android_device_oneplus_kebab（官方支持的最近 sibling）](https://github.com/LineageOS/android_device_oneplus_kebab)
- [LineageOS/lineage_wiki（官方支持机型与构建文档）](https://github.com/LineageOS/lineage_wiki)
- [Abandon-Project/twrp_device_oneplus_PGP110（Ace Pro 专属 recovery + prebuilt dtbo/kernel）](https://github.com/Abandon-Project/twrp_device_oneplus_PGP110)
- [QQiuAaaa/ofrp_device_oneplus_ovaltine（OrangeFox）](https://github.com/QQiuAaaa/ofrp_device_oneplus_ovaltine)
- [tangalbert919/android_device_oneplus_ovaltine（早期 10T 移植）](https://github.com/tangalbert919/android_device_oneplus_ovaltine)
- [ssut/payload-dumper-go](https://github.com/ssut/payload-dumper-go) · [bkerler/oppo_ozip_decrypt](https://github.com/bkerler/oppo_ozip_decrypt)
- [一加官方：一加 Ace Pro 规格页](https://www.oneplus.com/cn/ace-pro) · [一加社区 AcePro/10T 安装 LineageOS 教程](https://bbs.oneplus.com/thread/1289554032762814466)

## 十、第二轮自提 blob 的可行路径（实测工具能力）

`LineageOS/android_tools_extract_utils` 的 `extract_utils/extract.py` 里有
`extract_payload_bin`、`extract_super_img`、`extract_erofs`、`extract_zip`、`--extract-factory`
→ **`extract-files.py` 可以直接吃 OTA zip**，不必手工 payload-dumper + 挂载。

约束：该脚本需要一棵源码树布局（`tools/extract-utils` 相对路径 + `hardware/oplus`、
`hardware/qcom-caf/sm8450` 等 `namespace_imports` 目录存在），所以只能在
**已 repo sync 的 Linux 机上跑**，且输入是本机 7.2GB 的 `PGP110_cn.zip` —— CI runner 放不下底包，
因此第二轮的正确分工是：**手机容器（arm64 Ubuntu）负责提 blob → 产物推到私有 blob 仓 → CI 只做编译**。

## 十一、第二轮 blob 提取的可行性实测（手机容器）

在 arm64 容器里搭了最小源码树（`tools/extract-utils` + 两棵设备树 + `hardware/oplus` + 社区 blob 树，
`/build/blobwork`，约 11GB），直接喂 `extract-files.py`：

| 试法 | 结果 |
|---|---|
| 喂 OTA zip（`extract-files.py /dl/PGP110_cn.zip`） | ❌ 2623 行 `file not found`（含 `abl.img` 等全部固件）→ 该版本 dump 模式要的是**镜像目录**，不是 zip |
| 喂镜像目录（`/dl/dump`，硬链接不占空间） | ✅ 走通到 `move_alternate_partition_paths`，但需要 `--allow-prohibited-files`（LineageOS 对 `license.lic` 之类受限文件的交互确认） |
| 加 `--keep-dump --keep-images --allow-prohibited-files` | ❌ 停在 `shutil.move` → `OSError: [Errno 30] Read-only file system: /dl/dump/vendor/odm`（proot + 该工具的路径搬移逻辑冲突） |

结论：**提取必须在真正的 Linux 文件系统上做**（proot 的 bind 与该工具的 partition 搬移不兼容）。
这和编译是同一条要求 —— 一台 x86_64 Linux（≥250GB）既能提 blob 也能编 ROM，一轮做完。
脚本已固化：`.fetch/extract3.sh`（改一行 `D=` 指到 ext4 本地目录即可复用）。
